# WCIT (WHAT COLORCODE IS THIS?)
## 概要
背景色のカラーコードをユーザが当てるゲーム
## 画面
### トップ
タイトル，開始ボタン，ゲームのパラメータ，コピーライト表示がある．
背景は白色．
パラメータには，許容する解答誤差(マンハッタン距離)の指定，出題範囲(カラーコード指定)を入力する．
### プレイ
トップの開始ボタン，ないしは結果画面のもう一度ボタンを押すとここに飛ぶ．
特定の色になった背景，解答のためのtextareaと解答ボタンがある．
### 結果
プレイ画面の解答ボタンを押すとここに飛ぶ．
この時，背景色はプレイ画面のまま．
解答が誤差の範囲内なら，正解，範囲外なら，不正解を表示する．
また，もう一度ボタンもあり，押すとプレイ画面に飛ぶ．
トップに戻るボタンもある．
この画面でも，パラメータを弄れるようにする．
## 遊び方
各パラメータを入力し，開始ボタンを押す．
その後，大々的に変更された背景色からそのカラーコードを推測して，解答する．
それが正解であれば，うれしく，不正解であれば，当たるわけないとキレる．
以上．
## 実装について
画面遷移を行わず，全て同一の画面で済ませたい．
JavaScript(main.js)を用いて，要素を置き換えることで実装が可能だと信じている．
また，解答誤差の計算は，RGB値を3軸に分け，三次元空間上の離散値のマンハッタン距離として行う．

# 作成方法
AIによるコーディングを試したかったため，一切コードを書かずに全て指示のみで作成する．